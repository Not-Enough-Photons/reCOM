#include "seal.h"

bool CZSealBody::CanTraverse(unsigned int moveType)
{
	bool containsEdgePoints;



	return containsEdgePoints;
}

bool CZSealBody::CanClimbWall()
{
	return CanTraverse(3) || CanTraverse(4) || CanTraverse(1) || CanTraverse(5);
}